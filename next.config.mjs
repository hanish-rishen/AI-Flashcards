/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'lh3.googleusercontent.com', 'googleusercontent.com'],
  },
};

export default nextConfig;